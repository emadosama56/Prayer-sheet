# سجل الصلاة — Prayer Sheet

تطبيق موبايل بسيط (Flutter) لتسجيل الصلوات الخمس كل يوم، ومراجعة سجل الأيام السابقة.
كل البيانات محفوظة على الجهاز نفسه — من غير إنترنت ولا حساب ولا أي بيانات بتطلع برّه الموبايل.

A simple Flutter app for logging the five daily prayers and reviewing past days.
Everything is stored on-device — no account, no network, nothing leaves the phone.

## الفكرة

- تفتح التطبيق تلاقي صلوات النهاردة الخمسة قدامك.
- تدوس على الصلاة اللي صليتها، تبقى ✓ خضرا ومعاها وقت التسجيل.
- تفتح التطبيق تاني في أي وقت وتكمّل باقي الصلوات.
- زرار **سجل الأيام** بيوريك كل الأيام اللي فاتت وكام صلاة اتسجلت في كل يوم.
- نسيت تسجّل صلاة إمبارح؟ تقدر تدوس على اليوم في السجل وتعدّله.

## المميزات

| | |
|---|---|
| شاشة اليوم | الصلوات الخمسة + دائرة تقدّم (مثلاً 3/5) ووقت تسجيل كل صلاة |
| شريط الأسبوع | آخر ٧ أيام في سطر واحد، تدوس على أي يوم فيهم تعدّله |
| سجل الأيام | كل الأيام المسجّلة من الأحدث للأقدم، وأي صلوات ناقصة فيها |
| إحصائيات | عدد الأيام الكاملة، إجمالي الصلوات، وسلسلة الأيام المتتالية |
| تسجيل الكل | زرار واحد يسجّل باقي صلوات اليوم |
| تذكير ذكي | إشعارات حوالين مواعيد الصلاة الحقيقية، ولو سجّلت اليوم كله بيسكت لبكرة |
| واجهة عربية | الواجهة كلها عربي ومن اليمين لليسار، ومعاها وضع ليلي |

## التذكير الذكي

الوضع الافتراضي للإشعارات:

- **قبل كل صلاة بنص ساعة** — لو الصلاة الى قبلها لسه مش مسجلة، يجيلك تنبيه باسمها.
- **بعد كل صلاة بنص ساعة** — لو لسه ما سجلتهاش، يفكرك تسجّلها.
- **لو سجّلت الخمس صلوات** — التذكير يسكت تماماً لحد بكرة.
- **السكوت بالليل** — مفيش أي إشعار من ١١ بالليل لـ ٦ الصبح (تقدر تقفله).

فيه كمان وضع تاني بسيط: **تذكير كل ساعتين** من غير مواعيد ولا موقع.

كل ده بيتظبط من شاشة الإعدادات، ومعاه تحكم في الصوت (تشغيل/إيقاف).

### مواعيد الصلاة

بتتحسب **على الجهاز نفسه** بمكتبة `adhan` (طريقة الهيئة المصرية العامة للمساحة)،
يعني شغالة من غير إنترنت ومن غير سيرفر يقع. المطلوب بس المكان:

1. الـ GPS الأول (لو سمحت بالإذن).
2. لو مرفوض أو مش متاح — تحديد المدينة من الإنترنت.
3. لو الاتنين فشلوا — القاهرة كقيمة افتراضية، فالتطبيق عمره ما يقف.

الإشعارات بتتجدول للأيام الجاية، وبتتحسب من أول وجديد كل ما تفتح التطبيق أو
تسجّل صلاة — لأن الإشعار اللي اتسلّم للنظام مش بيقدر يغيّر رأيه بعد كده.

## التشغيل

```bash
flutter pub get
flutter run
```

للبناء:

```bash
flutter build apk --release      # Android
flutter build ios --release      # iOS
```

## الاختبارات

```bash
flutter analyze
flutter test
```

## بنية المشروع

```
lib/
├── main.dart                    نقطة البداية + الـ Scopes (الوصول للبيانات)
├── theme.dart                   ألوان وثيم Material 3
├── models/
│   ├── prayer.dart              الصلوات الخمسة وأسماءها وأيقوناتها
│   ├── day_record.dart          سجل يوم واحد + مساعدات التواريخ
│   └── day_timings.dart         حساب مواعيد الصلاة على الجهاز
├── services/
│   ├── prayer_store.dart        الحفظ والقراءة والإحصائيات
│   ├── location_service.dart    الموقع: GPS ← إنترنت ← افتراضي
│   ├── reminder_schedule.dart   قواعد التذكير (كود صافي، متغطى باختبارات)
│   └── reminder_service.dart    الإعدادات وجدولة الإشعارات
├── screens/
│   ├── home_screen.dart         شاشة اليوم
│   ├── history_screen.dart      سجل الأيام
│   └── settings_screen.dart     الإعدادات
└── widgets/
    ├── prayer_tile.dart         صف الصلاة الواحدة
    ├── today_header.dart        كارت اليوم ودائرة التقدّم
    ├── day_dots.dart            النقط الخمسة لكل يوم في السجل
    └── day_editor_sheet.dart    تعديل أي يوم (النهاردة أو يوم فات)

tool/
├── generate_app_icon.py         كل أحجام الأيقونات من رسمة واحدة
└── generate_notification_sound.py  صوت التنبيه الخفيف
```

## كيف البيانات متخزنة

كل السجل بيتحفظ في `shared_preferences` تحت مفتاح واحد (`prayer_records_v1`) على شكل JSON:

```json
{
  "2026-09-11": { "fajr": "2026-09-11T05:12:00.000", "dhuhr": "2026-09-11T13:40:00.000" }
}
```

اليوم اللي مفيهوش أي صلاة مسجّلة مش بيتخزن أصلاً، فسنة كاملة من الاستخدام حجمها كام كيلوبايت بس.

## النسخ والتحميل

كل build بيتنشر كـ release برقم (`v1`، `v2`، ...) وبيفضل محفوظ.
وفيه كمان لينك ثابت بيشاور دايماً على آخر نسخة:

```
https://github.com/emadosama56/Prayer-sheet/releases/download/latest-apk/prayer-sheet.apk
```

رقم النسخة بيتاخد من `version:` في `pubspec.yaml` (الرقم اللي بعد `+`).

## ملاحظة عن البناء لأندرويد

الـ Gradle wrapper هنا مرفوع لـ 8.7 والـ Android Gradle Plugin لـ 8.3.2 عشان المشروع يبني على JDK 17 و JDK 21.
الإعدادات الافتراضية اللي بيولّدها `flutter create` (Gradle 8.3 / AGP 8.1.0) بتفشل على JDK 21.
