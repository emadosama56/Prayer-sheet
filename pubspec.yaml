name: prayer_sheet
description: "سجل الصلاة — تطبيق بسيط لتسجيل الصلوات الخمس ومتابعة سجل الأيام السابقة."
publish_to: 'none'

version: 1.5.1+8

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  flutter_local_notifications: ^17.2.3
  adhan: ^2.0.0+1
  geolocator: ^13.0.2
  http: ^1.6.0
  timezone: ^0.9.4
  flutter_timezone: ^4.1.1
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  google_sign_in: ^6.2.2
  cloud_firestore: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
