<?xml version="1.0" encoding="utf-8"?>
<!--
  Five tappable prayers and today's count. RemoteViews supports only a small
  set of widgets, so the five are laid out explicitly rather than generated.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp"
    android:layoutDirection="rtl">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="سجل الصلاة"
            android:textColor="@color/widget_text"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/widget_done"
            android:textSize="14sp"
            android:textStyle="bold"
    </LinearLayout>

    <TextView
        android:id="@+id/widget_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:textColor="@color/widget_muted"
        android:textSize="11sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        android:baselineAligned="false">

        <TextView
            android:id="@+id/prayer_fajr"
            style="@style/PrayerWidgetPill" />

        <TextView
            android:id="@+id/prayer_dhuhr"
            style="@style/PrayerWidgetPill" />

        <TextView
            android:id="@+id/prayer_asr"
            style="@style/PrayerWidgetPill" />

        <TextView
            android:id="@+id/prayer_maghrib"
            style="@style/PrayerWidgetPill" />

        <TextView
            android:id="@+id/prayer_isha"
            style="@style/PrayerWidgetPill" />
    </LinearLayout>
</LinearLayout>
